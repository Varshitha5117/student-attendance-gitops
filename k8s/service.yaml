apiVersion: v1
kind: Service
metadata:
  name: attendance-service
spec:
  type: NodePort
  selector:
    app: attendance
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
